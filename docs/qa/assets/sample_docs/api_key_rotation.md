# API key rotation

1. Open **Settings → API keys**.
2. Click **Rotate** on the active key.
3. Copy the new secret once; update SDK clients within 24 hours.
4. Revoke the previous key after rollout.

Never commit raw keys to git.
