# Canonical SemVer lives in /VERSION (single line).
# Keep in sync with backend/pyproject.toml, frontend/package.json, sdk/pyproject.toml,
# sdk/raginspector/__init__.py, and Docker image tags on release.
